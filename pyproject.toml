[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "modelscope"
version = "0.1.0"
description = "Model family filtering helpers"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/modelscope"]
