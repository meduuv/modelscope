# modelscope

Small, dependency-free helpers for filtering model metadata by family.

## Usage

```python
from modelscope import select_by_family
select_by_family(models, "llama")
```

Run `pytest` to execute the test suite.

License: MIT. Built by meduuv. `guns.lol/meduu`
